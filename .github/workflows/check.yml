name: Check
on:
  push
jobs:
  scalafmt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.4.0
      - name: Install Scalafmt
        uses: coursier/setup-action@v1.1.2
        with:
          jvm: temurin:1.17
      - name: Check scalafmtCheck
        run: sbtn scalafmtCheckAll scalafmtSbtCheck
